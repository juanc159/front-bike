export default interface IFixedAssetList {
    id?: number,
    company_id: number | null,
    code: string | null,
    name: string | null,
    state: boolean | null,
    group_name: string | null,
}
